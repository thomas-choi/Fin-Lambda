# Lambda layer dependency set for portAssetsHandler (python3.13, layer finPort313).
#
# Kept separate from requirements_cron.txt on purpose: that layer pins
# pandas==1.5.3 / SQLAlchemy==1.4.46 / numpy==1.26.4, none of which have cp313
# wheels. The two layers must never be merged or cross-attached -- the 3.13
# layer ARN is declared on portAssetsHandler only, never at provider level.
#
# Build with:  make finPort313.zip     (see Makefile -- it cross-builds for
# cp313/manylinux2014_x86_64 rather than resolving against the local
# interpreter, which is 3.10 in this WSL environment).

PyMySQL==1.1.1
python-dotenv==1.0.1
SQLAlchemy==2.0.36
pandas==2.2.3
numpy==2.1.3
requests==2.32.3
pytz==2024.2

lxml==5.3.0             # pandas.read_html parser
beautifulsoup4==4.12.3  # read_html fallback flavour
openpyxl==3.1.5         # SSGA SPY .xlsx cross-check; droppable via INDEX_CROSSCHECK=False
